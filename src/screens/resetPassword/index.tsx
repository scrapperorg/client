import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ResetPasswordScreen() {
  return (
    <Box>
      <Typography>Reset Password Screen</Typography>
    </Box>
  );
}